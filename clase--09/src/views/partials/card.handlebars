{{!-- Usamos un ciclo para recorrer un array de productos --}}

{{#each arrayProductos}}
    <div>
        <h2>Titulo {{this.nombre}} </h2>
        <p>Descripcion {{this.descripcion}} </p>
        <p>Precio {{this.precio}} </p>
    </div>
{{/each}}